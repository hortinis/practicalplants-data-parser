{
  "name": "practicalplants-data-parser",
  "version": "0.2.0",
  "private": true,
  "description": "Offline parser for the recovered Practical Plants archive.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "parse": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
